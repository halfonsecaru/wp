import { AlfAutocompleteConfigInterface } from '../interfaces/alf-autocomplete.interface';

/**
 * Configuración por defecto para el componente autocomplete.
 */
export const ALF_AUTOCOMPLETE_DEFAULT: Partial<AlfAutocompleteConfigInterface> = {
  disabled: false,
};
