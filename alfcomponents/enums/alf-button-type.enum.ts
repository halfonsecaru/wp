/**
 * Enum para tipos nativos de botón HTML
 * Define el comportamiento del botón dentro de un formulario.
 * 
 * @example
 * ```typescript
 * // En un componente Angular
 * type: AlfButtonTypeEnum.Submit // El botón actúa como disparador de formulario
 * ```
 */
export enum AlfButtonTypeEnum {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}
