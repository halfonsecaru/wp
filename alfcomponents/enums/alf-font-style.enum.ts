/**
 * Enum para estilos de fuente (Normal, Itálica, etc.)
 * Permite definir la inclinación visual del texto.
 * 
 * @example
 * ```typescript
 * fontStyle: AlfFontStyleEnum.Italic
 * ```
 */
export enum AlfFontStyleEnum {
  Normal = 'normal',
  Italic = 'italic',
  Oblique = 'oblique',
}
