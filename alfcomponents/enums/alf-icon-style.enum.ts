/**
 * Enum para estilos de renderizado de iconos
 * Permite alternar entre estilos minimalistas, planos o con volumen.
 * 
 * @example
 * ```typescript
 * // En un componente Angular
 * style: AlfIconStyleEnum.ThreeD // Aplica sombreados y degradados 3D
 * ```
 */
export enum AlfIconStyleEnum {
    Traditional = 'traditional',
    Colored = 'colored',
    ThreeD = '3d'
}
