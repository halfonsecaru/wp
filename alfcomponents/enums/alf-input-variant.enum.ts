/**
 * Enum para variantes visuales de componentes de formulario
 * Controla el estilo base de inputs, selects y textareas.
 * 
 * @example
 * ```typescript
 * // En un componente Angular
 * variant: AlfInputVariantEnum.Outlined // Estilo clásico con borde completo
 * ```
 */
export enum AlfInputVariantEnum {
  Outlined = 'outlined',
  Filled = 'filled',
  Underlined = 'underlined',
}
