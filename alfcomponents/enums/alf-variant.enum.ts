/**
 * Enum para variantes semánticas básicas
 * Define la intención visual o color principal del componente.
 * 
 * @example
 * ```typescript
 * // En un componente Angular
 * variant: AlfVariantEnum.PRIMARY // Genera color de marca principal
 * ```
 */
export enum AlfVariantEnum {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  DANGER = 'danger',
  WARNING = 'warning',
  INFO = 'info',
  GHOST = 'ghost',
  OUTLINE = 'outline'
}
